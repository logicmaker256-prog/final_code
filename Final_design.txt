=== ロボット宅配の仕様書 ===

ここで、下のエージェントを定義する

・試作ロジック：ロジック確認のためにつくるもの
・本番用ロジック：試作ロジックの結果を反映して
　作る最終目的地

本コードは、試作ロジックの動作を基本として改良・改善した本番用ロジックを開発することを目的とする

==== 試作ロジックの仕様書====

■地図

サイズ：16×16
場所指定=> (row, col)
原点(0,0) => 左上
全角マップ

・正確な地図を持つ為、地図の学習しない

※本仕様における「地図の学習しない」とは、建物・道・歩道などの静的地形を対象としないことを指し、人の検出・反映は学習対象とする。

・地図データは「受」で保管、
　ロジックエージェントは、「受」で地図データを受け取り、

　・人の位置を追記する渋滞マップ
　・A*で使用するルートマップ
　を作る
。
・通常は渋滞マップを使用し、A*のルート
　決定のみルートマップを使う。

【地図データ】

■■■Ｂ・・◆◆◆◆・・Ａ■■■
■■■■・・◆◆◆◆・・■■■■
■■■■・・◆◆◆◆・・■■■■
■■■■・②＃＃＃＃・・■■■■
■■■■・・＃＃＃＃②・■■■■
■■■■・・◆◆◆◆・・■■■■
・・・・・・◆◆◆◆・・・・・・
・・①・・・◆◆◆◆・・・・①・
◆＃＃◆◆◆◆◆◆◆◆◆◆＃＃◆
◆＃＃◆◆◆◆◆◆◆◆◆◆＃＃◆
◆＃＃◆◆◆◆◆◆◆◆◆◆＃＃◆
◆＃＃◆◆◆◆◆◆◆◆◆◆＃＃◆
・①・・・・◆◆◆◆・・・①・・
・・・・・・◆◆◆◆・・・・・・
■Ｃ■■・②＃＃＃＃・・受■■■
■■■■・・＃＃＃＃②・■■■■

【地図要素と移動コスト】

■：建物
　A*,ロジックエージェントに対し進入不可

＃：横断歩道=1

◆：道
　A*、ロジックエージェントに対し進入不可

・：歩道=1

【ギミック】
①②：信号
　移動コストは横断歩道と同じ
　ロジックエージェントの動きを妨げない

◯(CNN未検知な人)：
　ロジックエージェントは
　人=100（エピソード終了)
　A*には見えていない。

◎(CNN検知済の人)：
　ロジックエージェントは
　人=100（エピソード終了)
　A*には論理進入不可

【場所】
ＡＢＣ：配送場所=1
受：受け渡し場所=1

・max_steps = 300 ～ 500とする
・max_stepsは実験条件により可変とする

【ロジックエージェント】
・キャラクタは次を使う
　上:▲,左:◀,下:▼,右:▶

・ロジックエージェントが用いる
　CNNの検知は、
　N = 5（前後左右2マス）とする

・人の出現場所は、毎エピソード変わり
　「受」でもらう地図データには記載
　されない。

・ロジックエージェントは、人との衝突判定は
　前進1マス確認で行い感知した「人」を、
　渋滞マップの同位置に◎を記入する。

・自分の場所は、地図とオドメトリ
　で誤差なくわかる。

・配送順序は重量と距離に基づき決定
・各区間の移動経路は A* により分かる


■ロジックエージェントについて

①エージェントは「受」にセットされ、
　内部のジャイロコンパスを用いて方向が
　分かる
②エージェントは左右に独立回転する
　タイアで動くためその場回転できる
③エージェントは、「受」から動き始める
　配送物の重さによって、配送順を計画
　し、A,B,Cをなるべく重い荷物から受け
　渡すように巡る。
　最後は、「受」に戻る

■「人」仕様

・人は静止

・人の配置パターン自体は毎エピソード変化するため学習対象としない

・ただし、人を検出し、回避・反映する行動は学習対象とする

・「人」の配置時の表示は「◯」

 [エージェントにとって]
・同一マス侵入で大ペナルティ
・エージェントと人の隣接はOK

 [A*にとって]
・◯は未検知
・◎は論理進入不可のオブジェクト
・CNNで感知した人(◯)のみ、渋滞マップ
　に(◎)として追記する。

■「人」配置アルゴリズム

・「◯」(人)は、エピソード開始時に配置
　される。
・A*は、◯を未検知オブジェクトとして
　扱い、◎のみを論理進入不可とする。

・渋滞マップに追記する。

・配置条件：横断歩道・歩道のみ配置

・人配置はランダムだが、
　マンハッタン距離D以内に
　配置禁止マークが存在しないよう
　候補セル集合を逐次削除する方式を
　採用する

配置禁止マーク：
　　Ａ・Ｂ・Ｃ・受・◯・◎

配置禁止距離 D = 2（マンハッタン距離）

以上により、エージェントが適切に回避
すれば、経路が完全に閉塞する事は
起きにくい配置を保証する。

■ 人回避アルゴリズム（安定版）

・人との衝突判定は、CNNの出力ではなく
　「次に実際に前進する1マス」に
　対してのみ行う。

・エージェントは、回転動作中は人との衝突が発生しないため、人検知を行わない。

・前進アクションを実行する直前に、
　次マス (nr, nc) を確認し、

　次マスに人（◯）が存在する場合：
　　・そのstepでは前進せず停止する
　　・該当セルを渋滞マップ上で ◎ に
　　　置換する
　　・A* による再探索を行う

・この判定は「前進 step のみ」で行い、
　人検知処理は1 step に1回だけ実行
　される。

・CNN は将来の学習用観測（周囲NxN）として利用可能だが、現仕様では衝突判定には直接使用しない。

■ CNN の役割

・CNN(5×5) は、ロジックエージェント
周囲の状況を観測するためのセンサ表現
である。

※現行仕様では、CNNは
・衝突判定には使用しない
・観測および教師データのみに使用する

・CNN はロジックエージェントの向きを
　基準に回転する。

・ただし、人との衝突判定は CNN では
　行わず、実際に侵入する1マスの確認
　により行う。

・CNN 出力は、将来的に
　観測（Observation）として
　強化学習に組み込むことを想定する。

■A*ルートをロジックエージェントが使うアルゴリズム

・地図は、ルートマップを使う。

・A*は通常ルートマップを使用する。
　人検知により渋滞マップが更新された場合、
　再探索時は渋滞マップを使用する。

・A*は、座標表現、ロジックエージェントは、前進・回転で表現するためビットローテーションによる変換をいれる

ロジックエージェントは、CNN(5×5)を用いて、周囲の人の存在を観測できるが、
人との衝突判定は実際に前進する1マスの
事前確認によってのみ行う。

a.A*座標配列　(row(i),col(i))
　→axisWithDirect(dir(i),row(i) ,col(i))変換

①今のA*座標を確認:(row(i),col(i))

②次のA*座標(row(i+1), col(i+1))に対し、

 人(◯)がいる場合は、人回避アルゴリズム
 人(◯)いない場合は、以下のアルゴリズム

  col差分で東西、row差分で南北を判定
　　col軸が-1ならば、dir(i)=0010B(西)
　　col軸が+1ならば、dir(i)=1000B(東)
　　row軸が-1ならば、dir(i)=0001B(北)
　　row軸が+1ならば、dir(i)=0100B(南)

③①と②からaxisWithDirect()に変換

b.axisWithDirect()からロジックエージェントの動きに変換

①axisWithDirect(dir(l),row(i),col(i))を読み込み

②ロジックエージェントの方向とdir(i)の方角から、次のロジックエージェントの動きを決定する
　　同方向　　　　:前進
　　ROLで同方向　:左回転,前進
　　RORで同方向　:右回転,前進
　ここで左回転／右回転アクションは
　「回転＋前進」を1 step として扱う
③信号待ちにて停止する。優先度は前進
　より勝る。停止中は動きの変換を
　進めない

 [ROLとRORの定義]
・左ローテーション(ROL)
0001 (北)
 ↓ ROL
0010 (西)
 ↓
0100 (南)
 ↓
1000 (東)
 ↓
0001 (北)  ← 循環

・右ローテーション(ROR)
0001 (北)
 ↑ ROR
1000 (東)
 ↑
0100 (南)
 ↑
0010 (西)
 ↑
0001 (北)

・A*方向（ビット対応）の定義
0: 北 (0001)
1: 西 (0010)
2: 南 (0100)
3: 東 (1000)

A*方向が未定義の場合
・obs上は -1 または 特殊値の場合、A*方向
　がない理由を、Env内部管理で区別する
・ロジックエージェントは人回避行動のみ
　で行動

・ロジックエージェント動き定義
0: 前進
1: 左回転
2: 右回転
3: 停止

■信号(①,②)について

【信号の定義】
・色は、CNNでわかる
・青信号：横断歩道を進める
・黄信号：停止位置で止まり、横断歩道に
　　　　　進めない。
　　　　　渡っている場合は渡り切る
・赤信号：停止位置で止まり、横断歩道に
　　　　　進めない

・①と②の赤信号と青信号のタイミングは
　逆になる。黄信号のタイミングは同じ

※本仕様では、信号周期を
青20step / 黄10step / 赤20step の50step周期とする。
旧仕様の15/6/15は廃止する。

・信号は20ステップの青信号、
　10ステップの黄信号と、
　20ステップの赤信号が切り替わる

・信号色はロジックエージェントのカメラ
　でわかるとし、実装では信号色(赤,黄,
　青)は、CNNでわかる

・信号①と②は25Timeステップの時間差
　がある
・建物の中は通れない
・信号を「青」でわたる事は、ルールと
　して知っているので学習対象としない。
・赤信号で止まることも、ルールとして
　知っている。
・止まる位置は、ロジックを使う。

■信号活用ルール

・A*がしめすルートが横断歩道の通過を
　予定している時、ロジックエージェント
　が、その横断歩道の信号をCNNで検出
　する。
　ロジックエージェントがたまたま信号の
　横を通っただけなのは、CNN判定しない

・信号の位置は、渋滞マップから分かる

・エージェント位置から信号セルまでの
　マンハッタン距離が4以下になつたらCNNで
　信号の色を確認する

・赤信号でロジックエージェントが停止
　するのは、

　・人がいない歩道
　　かつ
　・横断歩道か信号に接している所

・停止位置とは、
　横断歩道に隣接する歩道セルを指す。

■道「◆」のA*とロジックエージェントの違い

本仕様では、道(◆)は
A*・ロジックエージェント・模倣エージェントすべて進入不可とする*
将来的な拡張候補としてのみ「理論上進入可能」と記す。

■ペナルティの目安
・時間罰      : -0.01 / step
・道ペナルティ: -4.0 
・人接触      : -100（エピソード終了）

・重量ペナルティ：
換算式を示す
step_penalty = -0.01 * (1 + 重量係数)

重量係数は、↓の様に正規化して使う
重量係数 = 重量 / 最大重量
最大重量 = 20kg
重量係数のレンジ:0.0 ～ 1.0

・信号待ちのペナルティ
赤 + 停止 → ペナルティなし
赤 + 前進 → 大ペナルティ
青 + 停止 → 小ペナルティ（時間罰のみ）

■観測（Observation)

obs = {
  自己位置(row, col),
  向き(0-3),
  次のA*方向(0-3),
  信号色(one-hot),
  CNN観測(N×N)
}

・人との相対位置(周囲NxN)は、
　将来CNN出力を観測に含めるための
　設計項目であり、現コードでは未使用

■Envの内部管理
A*方向がない理由の為、astar_stateを導入

astar_state = "NORMAL"   # A*通り進め
astar_state = "AVOID"    # 人回避中
astar_state = "GOAL"     # ゴール到達
astar_state = "FAILED"   # A*失敗

・obs の A*方向が -1 の場合は astar_state で理由を区別する

・astar_state="FAILED" のとき
最大10stepは探索行動
それでも復帰できなければエピソード終了

・探索行動とは、回転のみを許可し前進は行わない行動を指す

■数値ID（Env内部用）

0 : 未探索
1 : 歩道
2 : 横断歩道
3 : 道
4 : 建物
5 : 配送先(A,B,C)
6 : 受
7 : 人(◯)
8 : 人(◎)
9 : 信号(セル)

・信号色はEnv内部で管理される
・ロジックエージェントはCNN相当の観測
　として、信号色(one-hot)を取得する

■gif / 可視化時のレイヤー順

0:地図（建物・道・歩道）
1:横断歩道
2:信号
3:人（◯ / ◎）
4:配送先・受
5:ロジックエージェント


■配送ルートのアルゴリズム

①目的地
　目的地は A, B, C の3カ所として、
　全順列を生成する（全6通り）

　受→A→B→C→受　受→A→C→B→受
　受→B→A→C→受　受→B→C→A→受
　受→C→A→B→受　受→C→B→A→受


②距離
d(X→Y)：地点Xから地点Yまでの移動距離
※距離はA*などの経路探索結果を用いる。

③重量コスト

wA, wB, wC：それぞれ目的地A, B, Cに配送する荷物の重量であり、

・各場合への配達物は、以下からランダム
　に選ばれ、それぞれに重量を持つ。

・ダンベル：20kg
・机：5kg
・PC部品：1kg
・ポケモンカード：0.1g

※重量は内部的に kg に正規化して扱う
（0.1g = 0.0001kg）

その移動コストは、各移動区間における
「移動距離 × 移動時点での積載重量」
の総和として定義する。

移動コスト =
  d(受→A)     × (wA + wB + wC)
+ d(A→B)     × (wB + wC)
+ d(B→C)     × (wC)
+ d(C→受)   ×  0

*距離 d(X→Y) は、
 ・A*により得られた地図上での経路探索
　結果を用いる。
・マップ依存である為、マップ読み込み
　時に計算し、その値を使用する
※距離 d(X→Y) はエピソード開始前に
　静的マップから一度だけ計算し、
　配送順序決定では再計算しない。

 d(受→A) d(受→B) d(受→C)
 d(A→B)  d(B→C)   d(A→C)

　方向性なしd(A→B)=d(B→A)
　信号・人などの動的要素は距離計算に含めない

④基本ルートの決定

　移動コストが最小となる配送順序を採用

・人回避などの動的障害物は、
  各区間の経路探索に反映されるが、
  配送順序決定ロジック自体は影響を受けない。

「重い荷物を持っている間の移動距離」を
最小化する配送順序を選択する。

======================

以下に試作ロジックのデバッグコード
delivery_logic.pyを示す。

==== delivery_logic.py ====

# ==========================================
# SLAM型配送：人回避＋信号＋配送順序決定（完成版）
# （荷物重量ランダム反映・正解パターン）
# ==========================================
import random, heapq, time, copy
from itertools import permutations

# ===== マップ（全角） =====
RAW_MAP = [
"■■■Ｂ・・◆◆◆◆・・Ａ■■■",
"■■■■・・◆◆◆◆・・■■■■",
"■■■■・・◆◆◆◆・・■■■■",
"■■■■・②＃＃＃＃・・■■■■",
"■■■■・・＃＃＃＃②・■■■■",
"■■■■・・◆◆◆◆・・■■■■",
"・・・・・・◆◆◆◆・・・・・・",
"・・①・・・◆◆◆◆・・・・①・",
"◆＃＃◆◆◆◆◆◆◆◆◆◆＃＃◆",
"◆＃＃◆◆◆◆◆◆◆◆◆◆＃＃◆",
"◆＃＃◆◆◆◆◆◆◆◆◆◆＃＃◆",
"◆＃＃◆◆◆◆◆◆◆◆◆◆＃＃◆",
"・①・・・・◆◆◆◆・・・①・・",
"・・・・・・◆◆◆◆・・・・・・",
"■Ｃ■■・②＃＃＃＃・・受■■■",
"■■■■・・＃＃＃＃②・■■■■",
]

H, W = 16, 16

# ===== 方向 =====
DIRS = {
    0:(-1,0),  # 北
    1:(0,-1),  # 西
    2:(1,0),   # 南
    3:(0,1),   # 東
}
ARROW={0:"▲",1:"◀",2:"▼",3:"▶"}

# ===== util =====
def manhattan(a,b): return abs(a[0]-b[0])+abs(a[1]-b[1])

def find(ch):
    for r in range(H):
        for c in range(W):
            if grid[r][c]==ch:
                return (r,c)

# ===== 信号 =====
def signal_color(step, offset):
    t = (step + offset) % 50
    if t < 20: return 0
    if t < 30: return 1
    return 2

def signal_name(c):
    return ["青","黄","赤"][c]

# ===== 人配置 =====
def place_people(n=6):
    cand=[(r,c) for r in range(H) for c in range(W) if grid[r][c] in "・＃"]
    banned=set(find(x) for x in "ＡＢＣ受" if find(x))
    random.shuffle(cand)
    for r,c in cand:
        if any(manhattan((r,c),b)<=2 for b in banned):
            continue
        grid[r][c]="◯"
        banned.add((r,c))
        if sum(1 for r in range(H) for c in range(W) if grid[r][c]=="◯")>=n:
            break

# ===== A* =====
def astar(start,goal,map_):
    pq=[(0,start)]
    cost={start:0}
    prev={}
    while pq:
        _,cur=heapq.heappop(pq)
        if cur==goal: break
        for d in DIRS.values():
            nr,nc=cur[0]+d[0],cur[1]+d[1]
            if not(0<=nr<H and 0<=nc<W): continue
            if map_[nr][nc] in "■◆◎": continue
            ncst=cost[cur]+1
            if (nr,nc) not in cost or ncst<cost[(nr,nc)]:
                cost[(nr,nc)]=ncst
                heapq.heappush(pq,(ncst+manhattan((nr,nc),goal),(nr,nc)))
                prev[(nr,nc)]=cur
    if start==goal: return [start]
    if goal not in prev: return []
    path=[goal]
    while path[-1]!=start:
        path.append(prev[path[-1]])
    return path[::-1]

# =============================
# 初期化
# =============================
grid=[list(r) for r in RAW_MAP]
place_people()

agent=find("受")
dir=0
mode="ROTATE"
crossing=False

# =============================
# 配送順序決定ブロック（正解パターン）
# =============================

# --- 荷物マスタ（kg） ---
ITEMS = {
    "ダンベル": 20.0,
    "机": 5.0,
    "PC部品": 1.0,
    "ポケモンカード": 0.0001,  # 0.1g
}

# --- 各配送先にランダム割当 ---
WEIGHTS = {
    p: random.choice(list(ITEMS.values()))
    for p in ["Ａ","Ｂ","Ｃ"]
}

print("📦 各配送先の荷物重量(kg):", WEIGHTS)

POINTS = ["受","Ａ","Ｂ","Ｃ"]
POS = {p: find(p) for p in POINTS}

# 距離行列（事前計算）
DIST={}
for a in POINTS:
    for b in POINTS:
        if a==b: continue
        p=astar(POS[a],POS[b],grid)
        DIST[(a,b)] = len(p)-1

def total_cost(order):
    remain=sum(WEIGHTS[x] for x in order)
    cost=0
    cur="受"
    for nxt in order:
        cost += DIST[(cur,nxt)] * remain
        remain -= WEIGHTS[nxt]
        cur = nxt
    return cost

best_order=min(permutations(["Ａ","Ｂ","Ｃ"]), key=total_cost)
delivery_queue=list(best_order)
delivery_queue.append("受")

print("📦 配送順序決定:", " → ".join(delivery_queue))

goal = POS[delivery_queue[0]]

# =============================
# step loop
# =============================
for step in range(200):

    sig1 = signal_color(step, 0)
    sig2 = signal_color(step, 25)

    vis=copy.deepcopy(grid)
    vis[agent[0]][agent[1]]=ARROW[dir]

    print("\n"+"="*40)
    print(f"STEP {step}  目標:{delivery_queue[0]}")
    print(f"🚦 信号①:{signal_name(sig1)} 信号②:{signal_name(sig2)} 横断中:{crossing}")

    for r in vis:
        print("".join(r))

    if agent==goal:
        print(f"✅ 到達:{delivery_queue[0]}")
        delivery_queue.pop(0)
        if not delivery_queue:
            print("🎉 全配送完了")
            break
        goal=POS[delivery_queue[0]]
        continue

    path=astar(agent,goal,grid)
    if len(path)<2:
        print("❌ 経路なし")
        break

    nr,nc=path[1]
    dr,dc=nr-agent[0],nc-agent[1]
    next_dir=[k for k,v in DIRS.items() if v==(dr,dc)][0]

    if mode=="ROTATE":
        if dir!=next_dir:
            dir=next_dir
            print("🔄 回転")
            mode="MOVE"
            time.sleep(0.2)
            continue
        mode="MOVE"

    if mode=="MOVE":

        if grid[nr][nc]=="◯":
            grid[nr][nc]="◎"
            print("👀 人 → 停止")
            time.sleep(0.3)
            continue

        if grid[nr][nc]=="＃":
            sig = sig1 if nr < 8 else sig2
            if sig==2 or (sig==1 and not crossing):
                print("🚦 信号 → 停止")
                time.sleep(0.3)
                continue
            crossing=True

        if crossing and grid[agent[0]][agent[1]]=="＃" and grid[nr][nc]!="＃":
            crossing=False
            print("🚶 横断完了")

        agent=(nr,nc)
        print("➡ 前進")
        mode="ROTATE"
        time.sleep(0.2)


=================================
               本番用ロジックの仕様書
=================================

試作ロジックの実装により明らかになった課題を反映したもの

・試作ロジックと本番用ロジックの違い

試作ロジック用固定マップ

・サイズ：16×16
・人マーク：未確認○、確認済◎
・表示：アスキーマップ
　マップ全体表示
・信号アスキーは、信号色によらず固定
　(縦方向①、横方向②)

本番自動生成マップ

・サイズ：32×32(将来も32が最小)
・人マーク：
　未確認(下歩道)ｓ(下横断歩道)ｚ
　確認済(下歩道)Ｓ(下横断歩道)Ｚ
・表示：PNGファイルから成る表示用マップ
　(アスキーマップは、コードの進行で用いるため、
　論理メモリー内で処理し、求めがない限り、
　表示しない)
　エージェント周囲7×7マスの表示
・信号アスキーと信号色によって6通りの
　バリエーションを持つ。
　他の挙動は試作ロジックと等しい

　アスキー ① 信号 赤 PNG signal_1_red.png
　アスキー ① 信号 黄 PNG signal_1_yellow.png
　アスキー ① 信号 青 PNG signal_1_blue.png

　アスキー ② 信号 赤 PNG signal_2_red.png
　アスキー ② 信号 黄 PNG signal_2_yellow.png
　アスキー ② 信号 青 PNG signal_2_blue.png

=== マップ生成の追加仕様書 ===

ここで、下のマップをエピソード毎に自動生成する
そのため、教師生成もランダムマップで作る
尚、信号の動作は試作ロジックの仕様に従う。


■地図

サイズ：32×32(本番用自動生成マップ)
場所指定=> (row, col)
原点(0,0) => 左上
全角マップ
ローグ風・都市生成

【地図要素と移動コスト】

■：建物
　A*,ロジックエージェント、
　模倣エージェントに対し進入不可

＃：横断歩道=1

◆：道
　A*,ロジックエージェント、
　模倣エージェントに対し進入不可

・：歩道=1

【ギミック】

①②：信号
　移動コストは横断歩道と同じ
　ロジックエージェント、
　模倣エージェントの動きを妨げない

ｓ,ｚ(CNN未検知な人)：
　ロジックエージェントは、人をさける。
　もし当たれば、100の罰を受ける

　A*には見えていない。
　(本仕様書の人回避アルゴリズムにて詳細明記)

Ｓ,Ｚ(CNN検知済の人)：
　ロジックエージェントは、人をさける。
　もし当たれば、100の罰を受ける

　A*には論理進入不可

【場所】
ＡＢＣ：配送場所=1
受：受け渡し場所=1

■生成マップの道について

・エージェントは、歩道と横断歩道のみ通れる。

・生成マップは、32×32とする(変更)

・各マップは、必ず構成要素としての 『道』を持つ(変更)

【縦貫通道路について】

・道はマップの上端を0として、
　4≦row(0)≦8と20≦row(1)≦24の位置に
　2本発生する。

・道は
　左上(row(i),0)-右下(row(i)+3,31)
　からなる(i=0,1)
・道同士は、8マス以上離れている

【横貫通道路について】
・道はマップの左端を0として、
　4≦col(0)≦8と20≦col(1)≦24の位置に
　2本発生する。

・道は
　左上(0,col(j))-右下(31,l_col(j)+3)
　からなる(j=0,1)
・道同士は、8マス以上離れている

【交差点の指定】
・縦貫通道路と横貫通道路は、
　4×4コマの道(◆)で交わり、交わった
　4×4の左上を(row(i),col(j))として
　以下の位置を示す

横断歩道は次の位置にある

　左上(row(i),col(j)-4)-右下(row(i)+3,col(j)-3)
　左上(row(i),col(j)+6)-右下(row(i)+3,col(j)+7)
　左上(row(i)-4,col(j))-右下(row(i)-3,col(j)+3)
　左上(row(i)+6,col(j))-右下(row(i)+7,col(j)+3)

次の4箇所に信号「①」を設置する

　(row(i)-1,col(j)-3)　(row(i)+4,col(j)-4)
　(row(i)-1,col(j)+7)　(row(i)+4,col(j)+6)

次の4箇所に信号「②」を設置する

　(row(i)-4,col(j)-1)　(row(i)-3,col(j)+4)
　(row(i)+6,col(j)-1)　(row(i)+7,col(j)+4)

道の脇には歩道があり、各道にはそれをわたる横断歩道がある

全ての歩道の幅は2セルとする。

全ての横断歩道の幅は、2セルとする。

道の幅は、必ず4セルとする

■ マップ生成手順

a.全面を建物で初期化

b.縦横に道路ラインをランダム生成

・2本の縦貫通道路
・2本の横貫通道路
・4箇所の交差点

c.道路両側を2セルの幅を持つ歩道に変換

d.横断歩道は2セルの幅を持つ。

e.交差点には信号必須

　信号設置は先の数式指定に従っ

f.受を配置

g.A/B/Cを配置

h.最終的な建物比率は全1024ル(32×32)14%以上となる

　 見た目バランスの為、建物比率は14%以上とする
　建物は必ず一辺は歩道と接する
　最終的に歩道と接していない建物があれば、 歩道が接する様に変換する

i.宅配の集配場は、建物内部にあるため、
　受とA,B,Cは、歩道に面した建物にある
　(歩道ではない)

j.受とA,B,Cの全移動先(6種)へ到達可能な通路があるかをA*で検証。無ければ、マップを再生成

k.人配置後に、受からのBFSチェック(下記)をして、宅配先にいけない場合は、マップを再構成

①受から探索
②到達できるセルをマーキング
③A,B,C が含まれているか確認

以下、追加仕様(7×7png表示による)

l.マップ外を示すｎを以下に付与する事で、マップ外は "ｎ" となる

ｎを付与する位置
　左上(-3,-3)-右下(-1,34)
　左上(0,-3)-右下(31,-1)
　左上(0,32)-右下(31,34)
　左上(32,-3)-右下(34,34)


■ レイヤー生成の順番

①建物ブロック生成
②道路を掘る
③歩道を付与
④横断歩道を付与
⑤信号を付与
⑥A/B/C/受を配置

======= 人表示を歩道と横断歩道に分けた事による人配置の変更仕様書 ========

マップのサイズ変更と自動生成に対して人の発生数を変更する。

■人配置アルゴリズムの変更(人発生数の変更)

①生成マップで歩道と横断歩道のマス目をxとしてカウントする
②4マスに1人ならばマンハッタン距離2以内を満たし、受/A/B/Cの入口には人を置けないので

　最大人の発生数　h_max=x/4-4

③人は歩道か横断歩道にあらわれるので、
歩道は「ｓ」マーク、横断歩道は「ｚ」で表して下さい

　人は、エピソード開始時に配置し、初期配置から移動しない

④A*は、ｓ及びｚを未検知オブジェクトとして
　扱い、Ｓ及びＺのみを論理進入不可とする。

⑤見つけた人を、渋滞マップに歩道は「Ｓ」
　横断歩道は「Ｚ」として追記する。

⑥配置条件：横断歩道・歩道のみ配置

⑦人最大数　h_maxまでのランダム数として
　人の人数は発生する

⑧人の配置はランダムだが、
　マンハッタン距離D以内に
　配置禁止マークが存在しないよう
　候補セル集合を逐次削除する方式を
　採用する

配置禁止マーク：
　　Ａ・Ｂ・Ｃ・受・ｓ・Ｓ・ｚ・Ｚ

配置禁止距離 D = 2（マンハッタン距離）

⑨実際に配置した人の数(h_total)を表示する

⑩以上により、エージェントが適切に回避
すれば、経路が完全に閉塞する事は
起きにくい配置を保証する。

===== PNGファイルを用いた追加仕様 =====

本テストは、マップ要素をPNGファイルに変換でき
るかをテストする為のものである。

・使用するPNGファイルは、Google driveの
　delivery_PNGフォルダにある。

・実行結果は、Google drive上にdelivery_view
　フォルダを作り、透明を維持する為、
　pngファイルで保存する。

・マップデータは32×32にマップ外を示す6を加えて
　38×38(0…37)となる

・マップ要素に対するPNGファイル(100×100Pixel)の対応を示す

・道　　　　◆　road.png
・横断歩道　＃　zebra.png
・歩道　　　・　sidewalk.png
・建物　　　■　building.png
・配送所　　受　delivery_center.png
・ポイントＡ　Ａ　point_A.png
・ポイントＢ　Ｂ　point_B.png
・ポイントＣ　Ｃ　point_C.png

信号は①と②の2つがあり、それぞれ赤信号、
黄信号、青信号を持つ。

・①　　　赤信号　signal_1_red.png
　　　　　黄信号　signal_1_yellow.png
　　　　　青信号　signal_1_blue.png
・②　　　赤信号　signal_2_red.png
　　　　　黄信号　signal_2_yellow.png
　　　　　青信号　signal_2_blue.png

エージェントは、東西南北の方向を持つ

・東向き　robot_E.png
・西向き　robot_W.png
・南向き　robot_S.png
・北向き　robot_N.png

人はエージェントに見つけられているか、いないかと立っている場所で4つの状態があるため、4つのpngファイルがある

・ｚ　blurry_person_zebra.png
・Ｚ　person_zebra.png
・ｓ　blurry_person_sidewalk.png
・Ｓ　person_sidewalk.png

表示マップの外は以下のように示す

・ｎ： nothing.png

①生成マップの中からランダムで10点を
　選択(row,col)する。
　道・建物・人・ｎ がある場合は選択をやり直す

②[7×7](0…6)の配列データをpngデータ(100×100Pixel)で用意、自動生成マップから表示データ(700×700Pixel)を作成する。

・関数定義：Map_ASCII(row,col)
　マップデータのrow,colにあるアスキー
　を取り出す

・関数定義：ASCII_To_Png(【アスキー】)
　マップデータのアスキーに対応するPngを
　読み込む

・関数使用例
　ASCII_To_Png(Map_ASCII(row,col))
　row,colのアスキーに対応するPngを読み込む

表示マップ：700×700(0…699)
#表示マップは 7×7個のPNGファイルからなる

(row,col)を中心として
7×7の表示マップを作成する

【擬似コード】

for j=-3 to 3 step 1
    for i=-3 to 3 step 1
        png=ASCII_To_Png(Map_ASCII(row+j,col+i))
        読み込んたpngファイルを表示マップに
        組み合わせる
    next i
next j

③(row,col)のPNGの上に
エージェントPNGを重ねて表示する


■信号について(本番用ロジック)

【信号の定義】

・信号状態はマップとは別の辞書で管理し、
　マップ中の全ての信号①②に共通である

・エージェントは信号色によって、動作を変える

・青信号：横断歩道を進める
・黄信号：停止位置で止まり、横断歩道に
　　　　　進めない。
　　　　　渡っている場合は渡り切る
・赤信号：停止位置で止まり、横断歩道に
　　　　　進めない

・①と②の赤信号と青信号のタイミングは
　逆になる。黄信号のタイミングは同じ

・信号周期を青20step / 黄10step / 赤20step
 　の50step周期とする。

・信号①と②は25Timeステップの時間差
　がある


■信号活用ルール(本番用ロジック)

・A*がしめすルート上の横断歩道をエージェントが
　横断しようとする時、その横断歩道の信号色が
　わかる

・エージェントがたまたま信号の
　横を通っただけなのは、判定しない

・信号の位置は、渋滞マップから分かる

・エージェント位置から信号セルまでの
　マンハッタン距離が4以下になつたら信号の色を
　確認する

・赤信号でロジックエージェントが停止するのは、

　・人がいない歩道
　　かつ
　・横断歩道か信号に接している所


■その他

・A*は信号を無視
・エージェント移動時は信号に従う
